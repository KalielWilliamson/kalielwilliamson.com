// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Remove `base` and set `site` to the selected custom domain once DNS is connected.
	site: 'https://kalielwilliamson.github.io',
	base: '/kalielwilliamson.com',
});
